<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Daftar Tugas</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f0f2f5;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        .container {
            max-width: 800px;
        }
        h1 {
            color: #212529;
            text-align: center;
            margin-bottom: 30px;
            font-weight: 700;
        }
        .card {
            border-radius: 12px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        }
        .list-group-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 20px;
            border-color: #e9ecef;
            border-radius: 8px;
            margin-bottom: 8px;
            background-color: #ffffff;
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }
        .list-group-item:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.08);
        }
        .task-text {
            flex-grow: 1;
            font-size: 1.1rem;
        }
        .actions {
            display: flex;
            gap: 8px;
        }
        .btn-sm {
            padding: 5px 12px;
            border-radius: 6px;
        }
        .btn-warning {
            background-color: #ffc107;
            border-color: #ffc107;
            color: #212529;
        }
        .btn-warning:hover {
            background-color: #e0a800;
            border-color: #d39e00;
        }
        .btn-danger {
            background-color: #dc3545;
            border-color: #dc3545;
        }
        .btn-danger:hover {
            background-color: #c82333;
            border-color: #bd2130;
        }
        .no-tasks {
            text-align: center;
            color: #6c757d;
            font-size: 1.2rem;
            margin-top: 30px;
        }
    </style>
</head>
<body>
    <div class="container mt-5">
        <h1>Daftar Tugas Saya 📝</h1>

        <div class="card mb-4">
            <div class="card-body">
                <h5 class="card-title">Tambah Tugas Baru</h5>
                <form action="/todos/add" method="POST" class="d-flex gap-2">
                    <input
                        type="text"
                        name="task"
                        class="form-control"
                        placeholder="Masukkan tugas baru"
                        required
                    />
                    <button type="submit" class="btn btn-success text-nowrap">Tambah</button>
                </form>
            </div>
        </div>

        <% if (todos && todos.length > 0) { %>
        <ul class="list-group">
            <% todos.forEach(function(todo) { %>
            <li class="list-group-item">
                <span class="task-text"><%= todo.task %></span>
                <div class="actions">
                    <a href="/todos/edit/<%= todo.id %>" class="btn btn-warning btn-sm">Edit</a>
                    <form action="/todos/delete/<%= todo.id %>" method="POST" class="d-inline">
                        <button type="submit" class="btn btn-danger btn-sm">Hapus</button>
                    </form>
                </div>
            </li>
            <% }); %>
        </ul>
        <% } else { %>
        <p class="no-tasks">Tidak ada tugas yang tersedia.</p>
        <% } %>

        <a href="/" class="btn btn-primary mt-4">Kembali ke Beranda</a>
    </div>
</body>
</html>